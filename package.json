{
  "name": "@inherent.design/simulacrum",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r --parallel test",
    "lint": "pnpm -r --parallel lint",
    "format": "pnpm -r --parallel format",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "pnpm": {
    "overrides": {
      "@visx/*>react": "^19.0.0"
    }
  }
}
